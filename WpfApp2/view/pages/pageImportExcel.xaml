<Page x:Class="WpfApp2.view.pages.pageImportExcel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:WpfApp2.viewmodel.importExcel"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Import Excel">

    <Page.DataContext>
        <vm:ImportExcelVm/>
    </Page.DataContext>

    <Border Background="#F0F2F5" Padding="25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="{DynamicResource m_ImportTitle}" FontSize="22" FontWeight="ExtraBold" Foreground="#1A237E"/>
                <Separator Margin="0,8,0,0" Background="#BDBDBD" Height="1"/>
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>

                <UniformGrid Columns="2">
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="{DynamicResource m_Step1}" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#455A64"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" 
                                     IsReadOnly="True" Padding="8" VerticalContentAlignment="Center" BorderBrush="#CFD8DC"/>
                            <Button Grid.Column="1" Content="{DynamicResource m_SelectFile}" 
                                    Command="{Binding BrowseFileCommand}" 
                                    Padding="15,0" Margin="5,0,0,0" Cursor="Hand"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Margin="15,0,0,0">
                        <TextBlock Text="{DynamicResource m_Step2}" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#455A64"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox ItemsSource="{Binding SheetList}" 
                                      SelectedItem="{Binding SelectedSheet, Mode=TwoWay}"
                                      Padding="8" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Content="{DynamicResource m_ImportBtn}" 
                                    Command="{Binding InsertCommand}"
                                    Background="#2E7D32" Foreground="White" FontWeight="Bold"
                                    Padding="20,0" Margin="10,0,0,0" Cursor="Hand"/>
                        </Grid>
                    </StackPanel>
                </UniformGrid>
            </Border>

            <GroupBox Grid.Row="2" Header="{DynamicResource m_Preview}" Foreground="#546E7A" FontSize="13">
                <DataGrid ItemsSource="{Binding PreviewData}" 
                          AutoGenerateColumns="True" 
                          IsReadOnly="True"
                          Background="White"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F1F1F1"
                          RowHeight="35"/>
            </GroupBox>

            <Border Grid.Row="3" Margin="0,15,0,0" Padding="12" CornerRadius="6" Background="#FFF3E0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Message}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Message}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📢" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Message}" VerticalAlignment="Center" FontWeight="Medium" Foreground="#E65100"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Page>